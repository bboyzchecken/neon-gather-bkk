# deps / build output
node_modules/
dist/
.next/
.turbo/
out/
coverage/
apps/api/bin/

# runtime junk
*.log
.DS_Store

# asset pipeline raw cache (re-tune post-processing without re-billing BFL)
.asset-raw/
*.tsbuildinfo

# local env (holds the real BFL_API_KEY — never commit)
.env
